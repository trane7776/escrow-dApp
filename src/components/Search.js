const Search = () => {
  return (
    <header>
      <h2 className="header-title">Search it. Explore it. Buy it.</h2>
      <input
        type="text"
        className="header-search"
        placeholder="Enter an address, neighborhood, city, or ZIP code"
      />
    </header>
  );
};

export default Search;
